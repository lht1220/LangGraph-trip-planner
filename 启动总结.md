# LangGraph 多智能体旅行助手 — 启动与配置总结

> 本文档基于当前仓库代码整理，记录项目结构、已修复的兼容性问题、高德地图 MCP 的配置需求，以及前后端启动步骤。

---

## 1. 项目简介

这是一个后端 **FastAPI + LangGraph**、前端 **Vue 3 + TypeScript + Vite + Ant Design Vue** 的旅行规划应用。

后端用 LangGraph 的 `StateGraph` 编排 4 个智能体并行/串行协作：

- **景点搜索 Agent** / **天气查询 Agent** / **酒店推荐 Agent**：并行研究，调用高德地图 MCP 工具
- **行程规划 Agent**：汇总研究结果，生成多日行程 JSON

前端提供表单填写页（`Home.vue`）与结果展示页（`Result.vue`），支持行程增强、重排/调整某天、替换景点等功能。

---

## 2. 技术栈

| 层 | 技术 |
|----|------|
| 后端框架 | FastAPI 0.132 + uvicorn |
| Agent 编排 | LangGraph 1.0.2（`StateGraph` / `create_react_agent`） |
| LLM 接入 | LangChain 1.2.0 + `langchain-openai`（OpenAI 兼容协议） |
| 地图工具 | 高德地图 MCP（`langchain-mcp-adapters` 通过 `uvx amap-mcp-server` 启动 stdio） |
| 前端 | Vue 3 + Vite 6 + Ant Design Vue 4 + axios |
| 模型示例 | DeepSeek（`deepseek-chat`，通过 OpenAI 兼容接口） |

---

## 3. 已修复的问题（本次）

### 3.1 `create_agent` 导入错误（导致后端无法启动）✅ 已修复

`backend/app/agents/langgraph_agents.py` 原代码：

```python
from langchain.agents import create_agent   # ❌ LangChain 1.x 已移除
agent = create_agent(model=llm, tools=tools, system_prompt=..., debug=False)
```

在 **LangChain 1.x**（`langchain==1.2.0`）中 `langchain.agents.create_agent` 已被删除，导入阶段即抛 `ImportError`，整个后端无法启动。

修复方案：改用 LangGraph 的 `create_react_agent`（`langgraph.prebuilt`），并封装了带版本兼容的辅助函数 `_build_react_agent`：

```python
from langgraph.prebuilt import create_react_agent

def _build_react_agent(model, tools, system_prompt):
    try:
        return create_react_agent(model=model, tools=tools, prompt=system_prompt)
    except TypeError:
        return create_react_agent(model=model, tools=tools, state_modifier=system_prompt)
```

> 注意：为避免 `debug=False` 参数在部分版本不存在而报错，已移除该参数。

### 3.2 地图健康检查引用错误 ✅ 已修复

`backend/app/api/routes/map.py` 的 `health_check` 引用了不存在的属性 `service.mcp_tool._available_tools`，导致 `/api/map/health` 直接 503。

修复为：`"mcp_tools_count": len(service.tools)`

### 3.3 asyncio 嵌套事件循环崩溃（导致真实 MCP 工具悄悄降级为 mock）✅ 已修复

**现象**：首次请求触发工作流初始化时，日志出现
`RuntimeError: Cannot enter into task ... while another task ... is being executed`，且 `/api/map/health` 的 `mcp_tools_count` 为 0（实际用了模拟数据）。

**根因**：调用链 `trip 路由 → get_trip_planner_workflow() → TripPlannerWorkflow.__init__ → get_cached_amap_tools() → asyncio.run(create_amap_mcp_tools())` 发生在 **uvicorn 请求协程已运行的事件循环内**。`asyncio.run()` 不能在已有运行循环上再创建并运行新循环，于是抛错；而 `amap_mcp_tools.py` 的 `get_amap_mcp_tools()` / `get_amap_essential_tools()` / `wrap_async_tools._run` 三处都直接调 `asyncio.run`，全部踩坑，最终被 `try/except` 吞掉并降级成 mock。

**修复（两步）**：
1. 在 `backend/app/tools/amap_mcp_tools.py` 新增 `run_async(coro)` 辅助函数——检测当前是否已有运行中的事件循环，若有则把协程交给独立线程（拥有自己的事件循环）执行，否则直接 `asyncio.run()`。三处 `asyncio.run(...)` 改为 `run_async(...)`。
2. **实测仍失败，进一步定位**：原代码里的 `nest_asyncio.apply()` 会破坏 `anyio` 的事件循环检测，导致 MCP server 启动子进程时抛 `anyio.NoEventLoopError: Not currently running on any asynchronous event loop`，进而触发 mock 降级。因此**彻底移除** `amap_mcp_tools.py` 中全部 3 处 `nest_asyncio.apply()` 调用及 import（`run_async` 已能在独立线程中安全运行协程，无需 `nest_asyncio`）。

> 修复后实测：有 `AMAP_API_KEY` 且 `uvx` 可用时，真实高德工具**成功加载 16 个**，`/api/map/health` 的 `mcp_tools_count > 0`，真实调用能发到高德（`CallToolRequest`）。但高德若返回 `INVALID_USER_KEY` 属于 Key 本身问题，见 5.5。

---

## 4. 环境要求与解释器注意（重要）

- **Python** ≥ 3.10，本项目已在 **Python 3.14** 实测安装通过（LangChain 1.x 全家桶可装）。
- **Node.js** ≥ 18（建议 20+）。
- **uv / uvx**：用于拉起高德地图 MCP 服务（`pip install uv`）。

### ⚠️ 解释器不匹配是最常见的坑（本机已遇到）

本机存在多个 Python，命令解析容易对不上：

- **Git Bash 里的 `python` / `python3`** → 指向 WorkBuddy 自带的 **Python 3.13.14**（`~/.workbuddy/binaries/python/...`），**没有**本项目依赖。
- **Windows 的 `python.exe` / `py` 启动器** → **Python 3.14**（py 默认版本），依赖装在它的 user site-packages。

后果：

- 在 Git Bash 直接敲 `python` / `pip` 会用到 3.13，**找不到 langgraph 等包**。
- 直接敲 `uvicorn` 会报 `command not found`（因为 3.14 的 Scripts 不在 Git Bash 的 PATH 上）。

**统一解决办法**：所有 Python 命令都用 `py -3.14 -m <模块>` 方式调用，确保命中装了依赖的 3.14。例如：

```bash
py -3.14 -m pip install -r requirements.txt
py -3.14 -m uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload
```

或见第 6 节的虚拟环境方案（一劳永逸）。

### ⚠️ pip 安装时的冲突/警告基本都可忽略

实测 `pip install -r requirements.txt` 会打印几行警告，但**安装成功**，且与本项目无关：

- `fastmcp.exe ... not on PATH`：警告。本项目用 `uvx amap-mcp-server` 拉起 MCP，`uvx` 会自建隔离环境装 fastmcp，不依赖这个全局路径。
- `google-api-core ... requires requests>=2.33.0, but you have 2.32.5`：本机**其他项目**装的全局包冲突，不在本仓库依赖内。
- `hello-agents 0.2.9 requires openai<2.0.0, but you have 2.23.0`：`hello-agents` 是另一个全局包；**本项目反而需要 openai 2.x**（`langchain-openai` 要求），2.23.0 正是正确版本。
- `Defaulting to user installation ...`：只是装到用户目录，功能不受影响。

---

## 5. 配置

### 5.1 哪些必须配置？

| 配置项 | 是否必须 | 说明 |
|--------|----------|------|
| **LLM（`LLM_API_KEY` 等）** | ✅ **强制** | 见 5.4。后端启动时 `validate_config()` 与 `get_llm()` 都会校验 `OPENAI_API_KEY`，缺失直接启动失败；4 个 Agent 也无法创建。**没有 LLM 整个行程生成跑不了。** |
| `AMAP_API_KEY` | ⚠️ 想要真实地图数据才必须 | 缺失时自动降级为 mock 景点/天气/酒店，但**链路能跑通**（见 5.3） |
| `uv` / `uvx` | ⚠️ 真实地图数据才需要 | 缺失或联网失败 → 降级 mock（见 5.3） |
| `UNSPLASH_*` | ❌ 可选 | 仅影响行程配图，缺省不影响核心流程 |

> 简言之：**LLM 是硬门槛，高德 Key 是"要真实数据才需要"，配图 Key 可有可无。**

### 5.2 高德地图 MCP 工作原理

后端通过 `backend/app/tools/amap_mcp_tools.py` 在运行时拉起子进程：

```python
connection = {
    "command": "uvx",
    "args": ["amap-mcp-server"],
    "transport": "stdio",
    "env": {"AMAP_MAPS_API_KEY": settings.amap_api_key}  # ← 高德 Key
}
```

**你需要做的：**

1. 到 [高德开放平台](https://lbs.amap.com/) 申请 **Web 服务** 类型的 Key（注意不是 JS API Key）。
2. 在 `backend/.env` 中填写（参考 `backend/.env.example`）：

```ini
# 高德地图 API Key（Web 服务类型）—— 想要真实地图数据才必填
AMAP_API_KEY=你的高德Key

# ===== LLM（强制必填，三选一填写即可）=====
# 方案 A：千问 / 通义（阿里云百炼，OpenAI 兼容，推荐）
LLM_API_KEY=你的DashScope_API_Key          # 以 sk- 开头
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_ID=qwen-plus                     # 或 qwen-max / qwen-turbo

# 方案 B：DeepSeek（取消注释改用）
# LLM_MODEL_ID=deepseek-chat
# LLM_API_KEY=你的DeepSeekKey
# LLM_BASE_URL=https://api.deepseek.com/v1
```

> **千问注意**：Base URL 结尾的 `/v1` 不能漏；行程规划依赖 Agent 的**工具调用（function calling）**，建议用 `qwen-plus` / `qwen-max` 这类工具调用能力强的模型。
> **LLM 缺失会直接启动失败**：见 5.4。

3. 确保机器上已安装并可用 `uvx`（用正确的解释器）：

```bash
py -3.14 -m pip install uv
uvx --help        # 能正常输出即 OK（或 py -3.14 -m uvx --help）
```

4. 首次运行后端会自动 `uvx amap-mcp-server` 拉取并启动 MCP；需要联网下载一次。

### 5.3 降级行为（无需配置也能跑通 demo）

- `AMAP_API_KEY` 为空 → 日志报 `AMAP_API_KEY 未配置`，返回空工具列表。
- `langchain-mcp-adapters` 未安装 / `uvx` 不可用 / 联网失败 → 自动降级到 `create_mock_tools()`（北京模拟景点、天气、酒店）。
- 这样**没有高德 Key 也能启动并走通整个链路**，只是数据是模拟的。

### 5.4 LLM 配置（强制）与兼容说明

**为什么是强制的**：`app/api/main.py` 启动时调用 `validate_config()`，若 `OPENAI_API_KEY` 未配置直接抛 `ValueError`；此外 `app/services/llm_service.py` 的 `get_llm()` 在创建 `ChatOpenAI` 前也会校验并抛错，而 4 个 Agent 创建时都要调 `get_llm()`。所以没有 LLM Key，**后端起不来、行程生成不了**。

配置层支持两套别名（`backend/app/config.py`），最终统一映射为 LangChain 标准的 `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL`：

- `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL_ID`
- `DEEPSEEK_API_KEY`（仅 key，base/model 走默认或单独配）

即支持任意 **OpenAI 兼容接口**，已验证可用的有：

- **千问 / 通义（阿里云百炼）**：`LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 `qwen-plus` / `qwen-max` / `qwen-turbo`。
- **DeepSeek**：`LLM_BASE_URL=https://api.deepseek.com/v1`，模型 `deepseek-chat`。
- 本地 Ollama 等：把 base_url 指向本地 OpenAI 兼容端点即可。

### 5.5 高德 Key 的两种类型（最容易踩的坑）与排查

高德开放平台按"应用类型"区分 **Key**，`amap-mcp-server`（后端）与前端地图渲染用的是**完全不同的两种 Key，不能混用**：

| 用途 | 环境变量 | 高德 Key 类型 | 调用的接口 |
|------|----------|---------------|-----------|
| 后端 Agent 查 POI/天气/路线 | `backend/.env` 的 `AMAP_API_KEY` | **Web 服务** | `restapi.amap.com`（服务端 API） |
| 前端地图渲染 | `frontend/.env` 的 `VITE_AMAP_WEB_JS_KEY` | **Web端(JS API)** | `webapi.amap.com`（浏览器 JS） |

**现象与排查**：

- 若后端日志/真实调用返回 `INVALID_USER_KEY` 或 `INVALID_USER_CODE`，说明 `AMAP_API_KEY` 不是有效的「Web 服务」Key（常见为误用 JS API Key、Key 过期、或未给该 Key 启用 Web 服务权限）。→ 去高德控制台新建/更换一个「Web 服务」类型 Key，填入 `backend/.env` 的 `AMAP_API_KEY`。
- 自检命令（Windows，走本地代理；去掉 `-x` 直连）：
  ```bash
  curl -x http://127.0.0.1:7897 "https://restapi.amap.com/v3/assistant/inputtips?key=你的KEY&keywords=故宫&city=北京"
  ```
  正常应返回 POI 列表；若返回 `"infocode":"10001"` 类错误即 Key 无效。
- 若前端地图区域空白 / 浏览器控制台报 `INVALID_USER_SCODE` 或安全密钥相关错误：说明 `VITE_AMAP_WEB_JS_KEY` 是 **2021-12 之后**申请的，**必须配安全密钥**。`frontend/src/views/Result.vue` 的 `initMap()` 当前未设置 `securityJsCode`，需在 `AMapLoader.load` 之前加：
  ```js
  window._AMapSecurityConfig = { securityJsCode: import.meta.env.VITE_AMAP_SECURITY_CODE || '' }
  ```
  并在 `frontend/.env` 增加 `VITE_AMAP_SECURITY_CODE=你的安全密钥`（高德控制台「安全密钥」一栏获取）。老 Key（2021-12 前申请）无需此步。

---

## 6. 后端启动

### 方案 A（推荐）：用虚拟环境，避免解释器混乱

```bash
cd backend

# 1. 用正确的解释器建 venv（关键：指定 py -3.14）
py -3.14 -m venv .venv
source .venv/Scripts/activate          # Windows Git Bash 用这个
#   激活后 python / pip / uvicorn 都指向 3.14

# 2. 安装依赖
pip install -r requirements.txt
pip install uv                          # 高德 MCP 用

# 3. 配置环境变量
cp .env.example .env
#   编辑 .env，填入 AMAP_API_KEY 与 LLM_*（见第 5 节）

# 4. 启动
uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload
```

### 方案 B：不建 venv，直接用 `py -3.14 -m`

若不想建 venv，所有命令都显式指定 3.14（依赖已装在 3.14 的 user site-packages）：

```bash
cd backend
cp .env.example .env          # 先填好 Key
py -3.14 -m uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload
```

> ⚠️ **不要**直接敲 `uvicorn ...` —— Git Bash 里会报 `command not found`（3.14 的 Scripts 不在 PATH）。要不直接 `py -3.14 -m uvicorn`，要不走方案 A 的 venv。

启动后访问：

- API 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health
- 地图服务健康：http://localhost:8000/api/map/health

> `app/api/main.py` 已启用 CORS，允许 `http://localhost:5173` 等前端来源。

---

## 7. 前端启动

```bash
cd frontend

# 1. 安装依赖
npm install

# 2. （可选）配置后端地址
cp .env.example .env
#   VITE_API_BASE_URL 默认 http://localhost:8000

# 3. 启动开发服务器
npm run dev
```

默认地址：http://localhost:5173

构建生产版本：

```bash
npm run build      # 输出到 dist/
npm run preview
```

---

## 8. 已知问题 / 待办

1. **解释器不匹配（本机已踩）**：Git Bash 的 `python` 是 WorkBuddy 自带 3.13，装依赖用的是 Windows `py` 启动器的 3.14。命令一律用 `py -3.14 -m ...` 或建 3.14 的 venv，否则 `import langgraph` 失败 / `uvicorn: command not found`。
2. **高德 MCP 工具在 Windows 上的 stdio 拉起**：`uvx` 需可用（用 `py -3.14 -m uvx`）；若公司网络有代理，首次 `uvx amap-mcp-server` 下载可能超时，建议先手动 `py -3.14 -m uvx amap-mcp-server --help` 预热。
3. **`amap_service.py` 解析**：`backend/app/services/amap_service.py` 中部分响应解析逻辑标记为 TODO，使用直接 HTTP 调用路径时返回结构可能与 MCP 路径不同，建议优先走 MCP 路径（默认）。
4. **行程规划 Agent 工具为空**：planner Agent 创建时 `tools` 可能为空列表，`create_react_agent` 已支持空工具，可正常运行。
5. **无自动化测试**：仓库暂无测试套件，建议后续补充 workflow 的单元/集成测试。
6. **`nest_asyncio` 已移除**：原 `amap_mcp_tools.py` 依赖 `nest_asyncio` 嵌套运行 asyncio，但它会破坏 `anyio` 事件循环检测，已彻底移除（见 3.3）。
7. **高德 `INVALID_USER_KEY`**：后端 MCP 已能连通，但 `AMAP_API_KEY` 必须是「Web 服务」类型 Key；误用 JS API Key / 过期 / 未启用权限都会触发。排查见 5.5。
8. **前端地图 `securityJsCode`**：`Result.vue` 的 `initMap()` 未设置安全密钥，2021-12 后申请的 JS API Key 需补 `window._AMapSecurityConfig`。见 5.5。

---

## 9. 快速验证清单

- [ ] 用 `py -3.14 -m pip show langgraph` 能查到包（确认解释器对）
- [ ] `backend/.env` 已配置 **`LLM_API_KEY`**（强制）+ `AMAP_API_KEY`（真实地图数据需要）
- [ ] `py -3.14 -m uvx --help` 可执行
- [ ] 后端启动无 `OPENAI_API_KEY 未配置` 报错
- [ ] 后端 `/api/map/health` 返回 `mcp_tools_count > 0`（说明真实工具已加载）
- [ ] 后端真实调用高德**不**返回 `INVALID_USER_KEY`（否则 `AMAP_API_KEY` 类型/值有误，见 5.5）
- [ ] 前端 `npm run dev` 正常，打开 http://localhost:5173
- [ ] 填表提交后能返回真实行程结果（景点坐标真实分布，而非全在北京固定点）
- [ ] 前端地图能正常显示标记（若空白且控制台报安全密钥错误，见 5.5 配 `securityJsCode`）

---

_整理日期：2026-08-17（已根据实测安装与启动踩坑更新）_
